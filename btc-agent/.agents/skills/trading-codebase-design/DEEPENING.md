# Deepening prompts

- Can callers violate an invariant by composing low-level calls?
- Can transaction ownership move behind one public interface?
- Does the interface reveal implementation details callers should not know?
- Are errors typed or clear enough to preserve fail-closed behavior?
- Can replay and rollback be tested through the public seam?
- Does the module accidentally gain exchange or SELL authority?
